name: Plone Docker RedTurtle Fork Image
on:
  push:
    branches: [ build ]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: [4.3.19, 5.1.6, 5.2.4]
        os: [debian, alpine, python2, python36, python37]
        exclude:
        - version: 4.3.19
          os: python2
        - version: 4.3.19
          os: python36
        - version: 4.3.19
          os: python37
        - version: 5.1.6
          os: python2
        - version: 5.1.6
          os: python36
        - version: 5.1.6
          os: python37

    steps:
    - name: Extract version
      id: version
      env:
        VERSION: ${{ matrix.version }}
      run: echo ::set-output name=major::${VERSION%.*}

    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      id: buildx
      uses: docker/setup-buildx-action@v1

    - name: Cache Docker layers
      uses: actions/cache@v2
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-

    - name: Login to DockerHub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker images
      uses: docker/build-push-action@v2
      with:
        tags: redturtletech/plone:${{ matrix.version }}-${{ matrix.os }}
        context: ${{ steps.version.outputs.major }}/${{ matrix.version }}/${{ matrix.os }} 
        pull: true
        push: true
        builder: ${{ steps.buildx.outputs.name }}
        cache-from: type=local,src=/tmp/.buildx-cache
        cache-to: type=local,dest=/tmp/.buildx-cache
